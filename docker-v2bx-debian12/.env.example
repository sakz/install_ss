# 填写 V2bX GitHub Release 的精确 tag，例如 v0.4.0。
V2BX_VERSION=

# 私有 v.zip 下载地址；该文件不得提交到 Git。
V2BX_CONFIG_URL=

# 本地构建时保持默认；使用 GHCR 镜像时改为 ghcr.io/<owner>/v2bx-debian12:<tag>。
V2BX_IMAGE=v2bx-debian12:local
